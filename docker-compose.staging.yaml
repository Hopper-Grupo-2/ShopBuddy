version: "3.8"
name: "shopbuddystaging"

services:
    #redis:
    #    image: redis:7.0.9
    #    container_name: redis
    #    command: redis-server --requirepass ${REDIS_PASSWORD} --user ${REDIS_USER}
    #    restart: always
    #    ports:
    #        - "6379:6379"
    #    env_file:
    #        - .env

    mongostaging:
        build: ./server/src/database/
        container_name: mongo${NODE_ENV}
        restart: always
        ports:
            - "26:27017"
        volumes:
            - mongodb-data:/data/db
        env_file:
            - .env
            - server/config/.env

    appshopstaging:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: appshopstaging
        restart: always
        env_file:
            - server/config/.env
        ports:
            - "${PORT}:${PORT}"
        volumes:
            - .:/app/
        # environment:
        #   - REDIS_DB=redis
        #   - MONGO_DB=mongo
        depends_on:
            # - redis
            - mongo${NODE_ENV}
        command: npm run start

volumes:
    mongodb-data:

networks:
    shop_buddy:
        driver: bridge
